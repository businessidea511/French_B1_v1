{
    "buildCommand": "bash build.sh",
    "outputDirectory": "build/web",
    "framework": null,
    "installCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter doctor && ./flutter/bin/flutter precache --web && ./flutter/bin/flutter pub get"
}